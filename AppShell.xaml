<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="German_Project.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:German_Project"
    Shell.FlyoutBehavior="Disabled"
    Title="German_Project">

    <TabBar>
        <!--<Tab Title="Объявления" Icon="home">
            <ShellContent
                ContentTemplate="{DataTemplate local:Ads}"/>
        </Tab>-->

        <Tab Title="Расписание" Icon="home">
            <ShellContent
                x:Name="Schedule"
                ContentTemplate="{DataTemplate local:MainPage}"/>
        </Tab>

        <!--<Tab Title="Настройки" Icon="home">
                    <ShellContent
            ContentTemplate="{DataTemplate local:Settings}"/>
        </Tab>-->
    </TabBar>

</Shell>
